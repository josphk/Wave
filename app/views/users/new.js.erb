var form = $('.signup')
var formToggle = $('#toggle-form');

if (formToggle.text() === "Don't have a tracker?") {
  form.html("<%= j(render 'regular') %>");
  formToggle.text('Got a tracker?');
} else {
  form.html("<%= j(render 'particle') %>");
  formToggle.text("Don't have a tracker?");
}