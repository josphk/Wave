var form = $('.sign-in')
var formToggle = $('#toggle-form')

if (formToggle.text() === "Don't have a Wave Motion?") {
  form.html("<%= j(render 'regular') %>")
} else {
  form.html("<%= j(render 'particle') %>")
}